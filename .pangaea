#!/bin/bash

# OPTIONS
# ENVIRONMENT={development, production}
ENVIRONMENT=production
# PROVIDER={gce, vagrant}
PROVIDER=vagrant

# KUBERNETES
KUBE_LOGGING=true
KUBE_MONITORING=true
KUBE_GCR_CREDENTIALS=true

# GCE
GCE_INSTANCE_NAME=testinstance
