#!/bin/bash

# OPTIONS
# PROVIDER={gce, vagrant}
PROVIDER=gce

# KUBERNETES
KUBE_LOGGING=false
KUBE_MONITORING=false
KUBE_GCR_CREDENTIALS=false

# GCE
GCE_INSTANCE_NAME=testin
GCE_DISK_MOUNTS=(
#   diskname mountpath
)
